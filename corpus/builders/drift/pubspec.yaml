name: corpus_drift
description: drift_dev — só as saídas do build (o programa exigiria sqlite3 por FFI).
publish_to: none
environment:
  sdk: ^3.6.0
dependencies:
  drift: ^2.28.2
dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.28.0
